# ExoKey dependencies, pinned to the versions the gates pass on (Python 3.12).
# Install:  pip install -r requirements.txt
#
# Note: trimesh and scikit-image are REQUIRED for the STL export (manufacture/mesh.py
# marching-cubes -> watertight solid) even though older README prose omitted them.

mujoco==3.10.0
numpy==2.5.1
scipy==1.18.0
pymoo==0.6.2
PyNiteFEA==3.0.0
plotly==6.9.0
pytest==9.1.1

# STL export (marching cubes -> mesh)
trimesh==4.12.2
scikit-image==0.26.0

# CHOLMOD Cholesky for the FEM hot path (structure/fem.py) -- 2.4-3.1x per evaluate(),
# bit-identical F. OPTIONAL: fem.py falls back to scipy splu if this is absent.
# Needs libsuitesparse-dev at build time (apt).
scikit-sparse==0.5.0
